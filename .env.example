PORT=3000

# Not used yet — the app currently serves mock data with no database.
# Documented here so the connection point is obvious when a real DB is wired up.
DATABASE_URL=postgres://user:password@host/dbname
